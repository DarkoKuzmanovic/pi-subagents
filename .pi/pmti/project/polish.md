# PMTI Polish Backlog

- After M0 implementation, update user-facing docs/examples to show inline `thinking` only if the implementation changes documented API examples.
- Consider adding a compact `/subagents` help hint for thinking-level controls if the selector keybinding is not self-evident.
- Revisit legacy `.pi/tasks/` histories in M1 and promote only durable decisions/lessons, not full historical logs.
